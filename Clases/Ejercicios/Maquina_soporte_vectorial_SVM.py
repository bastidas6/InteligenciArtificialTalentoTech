#SVM = Es un algortimo de aprendizaje supervisado, para tareas de clasificación y regresión.
# El objetivo es encontrar la separacion entre los puntos, o un hiperplano optimo que maximice la separación de los puntos de datos

import pandas as pd
import numpy as np